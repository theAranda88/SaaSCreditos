{
  "info": {
    "name": "Creditos SaaS — local",
    "description": "Colección local del monorepo (Fase 0). Usar con el entorno `local`.",
    "schema": "https://schema.getpostman.com/json/collection/v2.1.0/collection.json"
  },
  "item": [
    {
      "name": "Salud",
      "item": [
        {
          "name": "GET /api/salud",
          "request": {
            "method": "GET",
            "header": [],
            "url": {
              "raw": "{{url_base}}/api/salud",
              "host": ["{{url_base}}"],
              "path": ["api", "salud"]
            },
            "description": "Verifica que la API responde correctamente."
          },
          "response": []
        }
      ]
    }
  ]
}
